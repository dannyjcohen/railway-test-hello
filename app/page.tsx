export default function Home() {
  return (
    <main>
      <h1>Hello from Railway auto-deploy test v7 — workflow with PROJECT+ENV IDs committed — build {new Date().toISOString()}</h1>
    </main>
  );
}
