export default function Home() {
  return (
    <main>
      <h1>Hello from Railway auto-deploy test v6 — full pipeline with PROJECT+ENV vars — build {new Date().toISOString()}</h1>
    </main>
  );
}
